# Pendiente: <the open point in one line>

> The description of a `Pendiente:` sub-task — one per point a relevamiento left open. It hangs off the
> relevamiento task in Jira and points back at the minuta; whoever picks it up should be able to answer
> it from this text alone, without reopening the conversation.

| | |
|---|---|
| **Raised during** | <Jira key of the relevamiento, as a link> — `Minuta <KEY> — <task summary>` (as a link), section *<section>* |
| **Raised by** | <name, role> on <YYYY-MM-DD> |
| **Owner** | <name, role — who can answer it> |
| **Needed by** | <date, or "before the next session", or "no fixed date"> |
| **Blocks design** | Yes / No — <one line why> |

## What is open

<Stated specifically enough that a precise answer is possible. Not "the approval flow" but "when a
discount above 20% is requested, who approves it and what happens if they are on leave?">

## Why it matters

<What this decides — a rule, a step, who sees what — and what the minuta cannot settle without it.>

## What we already believe

<The customer's current understanding, from the conversation. Stating it lets the owner correct a
wrong assumption directly rather than answer from scratch.>

## What was offered or agreed

<A document to send, an example to bring, a person to ask — whatever the customer committed to in the
room, so the sub-task records the promise as well as the question.>

## When it is done

<The answer written into the minuta's section named above — the `⚠️ TBD` replaced — and on this sub-task
as a closing comment.>
